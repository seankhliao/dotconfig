cicd () {
    gcloud builds log \
        --stream \
        $(gcloud builds list \
            --filter='source_provenance.resolved_repo_source.commit_sha="'$( \
                git rev-parse HEAD \
            )'"' \
            --format='value(id)' \
        )
}
cicd
